spring.application.name=equipment-management
server.port=8080

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/equipment_management
# OR if using MongoDB Atlas (cloud):
# spring.data.mongodb.uri=mongodb+srv://username:password@cluster.mongodb.net/equipment_management

# MongoDB Database Name
spring.data.mongodb.database=equipment_management

# Optional: Logging for debugging
logging.level.org.springframework.data.mongodb=DEBUG